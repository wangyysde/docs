-----
# Function List #
1. Default() *Engine   // (gin.go) Default returns an Engine instance with the Logger and Recovery middleware already attached.
			debugPrintWARNINGDefault()
			engine := New()
			engine.Use(Logger(), Recovery())
2. (engine *Engine) LoadHTMLGlob(pattern string)  //(gin.go) LoadHTMLGlob loads HTML files identified by glob pattern and associates the result with HTML renderer.